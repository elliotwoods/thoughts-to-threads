# =============================================================================
# Thoughts to Threads - environment variables
# Copy to .env.local for local dev, or set in the Vercel project settings.
# =============================================================================

# --- App ---------------------------------------------------------------------
# Public base URL of the deployment (no trailing slash).
APP_BASE_URL=                 # https://your-app.example.com
# Random secret. Vercel Cron sends it as: Authorization: Bearer ${CRON_SECRET}
CRON_SECRET=
# Base64-encoded 32 random bytes for AES-256-GCM token encryption.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=

# --- Microsoft (Entra app registration, delegated) ---------------------------
MS_CLIENT_ID=
MS_CLIENT_SECRET=
MS_TENANT=consumers           # "consumers" for personal-only, or "common"
MS_REDIRECT_URI=              # {APP_BASE_URL}/api/auth/microsoft/callback

# --- Threads (Meta app) ------------------------------------------------------
THREADS_APP_ID=
THREADS_APP_SECRET=
THREADS_REDIRECT_URI=         # {APP_BASE_URL}/api/auth/threads/callback

# --- Firebase Admin (service account) ----------------------------------------
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# Keep the literal \n escaping from the JSON key; it is unescaped at runtime.
FIREBASE_PRIVATE_KEY=

# --- Optional ----------------------------------------------------------------
# Only if using Ably push for live dashboard updates.
ABLY_API_KEY=
# Where re-auth / failure alerts POST to (e.g. a Slack/Discord webhook).
NOTIFY_WEBHOOK_URL=

# --- Cloudflare Access (optional defence-in-depth in middleware) --------------
# If both are set, middleware requires the Cf-Access-Jwt-Assertion header.
CF_ACCESS_AUD=
CF_ACCESS_TEAM_DOMAIN=
